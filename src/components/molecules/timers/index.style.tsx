import { View } from 'react-native';
import styled from 'styled-components/native';

export const TimersContainer = styled(View)`
    flex: 1;
    width: 100%;
    justify-content: space-between;
    flex-direction: row;
`;
